name: 🔄 批量同步Notion页面到GitHub
on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: 批量拉取Notion页面
        run: |
          pip install --force-reinstall notion2md==2.8.0
          mkdir -p ./notion-exports
          notion2md --token ${{ secrets.NOTION_TOKEN }} \
                    -n ${{ secrets.NOTION_DATABASE_ID }} \
                    -o ./notion-exports \
                    --download True
        continue-on-error: false
      - name: 提交推送
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add ./notion-exports/*
          if ! git diff --staged --quiet; then
            git commit -m "批量同步Notion页面 $(date +'%Y-%m-%d %H:%M:%S')"
            git push
          else
            echo "无页面变更，跳过提交"
          fi