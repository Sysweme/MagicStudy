name: 🔄 Sync Notion Page to GitHub

on:
  schedule:
    - cron: '0 2 * * *'  # 每天 UTC 2 点运行
  workflow_dispatch:      # 允许手动触发

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Fetch Notion Page as Markdown
        run: |
          pip install notion2md
          notion2md export --token ${{ secrets.NOTION_TOKEN }} \
                           --page_id ${{ secrets.NOTION_PAGE_ID }} \
                           --output_filename "notion-export"

      - name: Commit and Push
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add .
          git diff --staged --quiet || git commit -m "🔄 自动同步 Notion 页面"
          git push
