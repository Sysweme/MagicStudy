name: 🔄 Sync Notion Page to GitHub

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Fetch Notion Page as Markdown
        run: |
          # 锁定稳定版本 2.8.0，避免参数变更问题
          pip install notion2md==2.8.0
          mkdir -p ./notion-exports
          # 2.8.0 稳定参数：-t 令牌，-n 页面ID，-o 输出目录，--download 下载资源
          notion2md -t ${{ secrets.NOTION_TOKEN }} \
                    -n ${{ secrets.NOTION_DATABASE_ID }} \
                    -o ./notion-exports \
                    --download True
        continue-on-error: false

      - name: Commit and Push
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add ./notion-exports/*
          if ! git diff --staged --quiet; then
            git commit -m "🔄 自动同步 Notion 页面 $(date +'%Y-%m-%d %H:%M:%S')"
            git push
          else
            echo "无 Notion 页面变更，跳过提交"
          fi
