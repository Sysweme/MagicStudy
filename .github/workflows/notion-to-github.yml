name: 🔄 Sync Notion Page to GitHub

on:
  schedule:
    - cron: '0 2 * * *'  # 每天 UTC 2 点运行
  workflow_dispatch:      # 允许手动触发

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # 1. 配置 Python 环境
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # 2. 安装 notion2md 并导出 Notion 页面（修正 Secrets 名称）
      - name: Fetch Notion Page as Markdown
        run: |
          pip install notion2md
          # 创建输出目录
          mkdir -p ./notion-exports
          # 关键修正：改用实际的 Secrets 名称 NOTION_DATABASE_ID
          notion2md --token ${{ secrets.NOTION_TOKEN }} \
                    --id ${{ secrets.NOTION_DATABASE_ID }} \
                    --output ./notion-exports \
                    --download True  # 自动下载图片资源
        continue-on-error: false

      # 3. 提交并推送变更
      - name: Commit and Push
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add ./notion-exports/*
          if ! git diff --staged --quiet; then
            git commit -m "🔄 自动同步 Notion 页面 $(date +'%Y-%m-%d %H:%M:%S')"
            git push
          else
            echo "无 Notion 页面变更，跳过提交"
          fi
